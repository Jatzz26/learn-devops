name: second CI

on:
      # push
      workflow_dispatch
      
jobs: 
      build:
            runs-on: ubuntu-latest
                  
            steps:
                - name: Say hello
                  run: echo "Hello from the second CI workflow"